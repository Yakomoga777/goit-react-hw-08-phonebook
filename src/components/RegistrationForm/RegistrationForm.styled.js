import {
  StyledContactInput,
  StyledForm,
} from 'components/ContactForm/ContactForm.styled';
import { StyledButton } from 'components/ContactList/ContactItem.styled';
import styled from 'styled-components';

export const StyledRegForm = styled(StyledForm)``;
export const StyledRegInput = styled(StyledContactInput)`
  width: 200px;
`;
export const StyledRegButton = styled(StyledButton)`
  height: 40px;
  padding: 10px;

  line-height: 1;
`;
